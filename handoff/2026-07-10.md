# Handoff — 2026-07-10 (Claude Code session: Copilot audit review)

Branch: `claude/copilot-audit-review-homcvr` (user merges to main + deploys manually).

## Done today
- Reviewed `Github-Copilot-Doshusweb-Audit.md`: 3 real findings, several bogus (env vars impossible on static hosting, fabricated code snippets, SRI infeasible for dynamic scripts). Copilot MISSED the biggest bug (see Open Items #1).
- **CSP hardened + promoted**: enforced `script-src` now has NO unsafe-inline/unsafe-eval — inline scripts via sha256 hashes. Verified via Report-Only monitoring on prod (only browser-extension noise), then promoted, then removed the RO headers. **MDN Observatory: A+ 105/100** (was 45 → 75 → 105). Remaining dings (accepted): SRI (infeasible), `style-src 'unsafe-inline'` (needed by widget styles + glyph SVGs; recipe to tighten later is in DOSHUS.md playbook).
- **`npm run csp:hashes`** (`scripts/update-csp-hashes.js`): rescans inline scripts, rewrites hash tokens in firebase.json. Run after editing any inline `<script>`.
- **Corp links/email out of repo**: `home.js` builds Amazonian Spot pills + work email from RTDB `/config/worklinks` at runtime. Seeded + rules deployed + verified live. Old strings remain in git history only.
- **firebase.json now declares `database.rules.json`** — `firebase deploy --only database` works (was undeployable before; console rules matched repo exactly, no drift).
- Removed inert `firebase` npm dep. Google Fonts CSP entries kept — the CoinGecko widget needs them (not stale).
- DOSHUS.md has a lean **Security/CSP Playbook** — keep it lean per user's request.

## Open items (priority order)
1. **Chat orb stored XSS** — `renderContent()` in `public/js/zephyy.js` (~line 551) restores markdown link labels/URLs/img alts UNESCAPED into `innerHTML` (line ~600). Payload: `[<img src=x onerror=...>](https://x.co)`. Fix Part A (ready anytime, no coordination needed): escape label/url/alt at restore time or build with createElement/textContent; enforce http(s) schemes. Does NOT touch message format/RTDB paths.
2. **RTDB chat rules redesign (Part B)** — `zephyy/chat/sessions` world-readable, `messages` world-writable. Target: top-level read `auth !== null`, per-$sessionId read open (unguessable ID = capability), messages write open but `.validate`-constrained (or anon-auth-gated). **BLOCKED on Zephyy's answers** — user is relaying a 7-question questionnaire via Discord (agent write auth, session enumeration, ID entropy, message schema, Aether/agents-oc paths, control node purpose, anonymous-auth tolerance). Get answers before touching rules — her agents share these paths.
3. Later/optional: drop `style-src unsafe-inline` (fix glyph SVG inline styles first, stage via Report-Only recipe in DOSHUS.md); SRI pin for versioned gstatic Firebase SDK.

## Conventions this session established
- Handoff folder: append a `handoff/YYYY-MM-DD.md` per working day summarizing work + open items.
- Preview-first deploys (user deployed straight to prod once — gently nudge).
- `public/amazon/**` = quarantine: own loose CSP, excluded from hash scan, don't refactor.
- User is a beginner and appreciates explanations; teach while doing.
