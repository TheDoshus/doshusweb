# Handoff — 2026-07-12 (Claude Code session: chat orb XSS fix + RTDB rules lockdown)

Branch: `main` (worked directly on main — user had merged PR #22 mid-session).

## Done today
- **Part A — chat orb stored XSS fixed** (`ad1bb04`): `renderContent()` in `public/js/zephyy.js` now escapes markdown link labels/urls/img alts via new `escapeHtml()` at placeholder-restore time; also switched restores to function replacements ($-token corruption bug). Verified by extracting the real-file functions and running 9 payload/legit cases — all attacker markup inert.
  - ⚠️ Sync gotcha that happened: PR #22 merged the codex branch at `e5c28a6`, one commit *before* the XSS fix — the fix had to be cherry-picked onto main (`65ab868` → `ad1bb04`). The codex branch's local tip still has the orphaned original commit; harmless.
- **Part B — RTDB chat rules redesigned** (`81688a7`), from Zephyy's traced answers (verified against code — she was accurate except one item, see below):
  - `sessions` top-level world-read removed (no public enumeration). Backend (orb, Aether) uses the DB secret which **bypasses rules**, so server-side reads are unaffected.
  - `messages`: create-only per-`$msgId` writes, validated `{role, content, timestamp}`, role locked to `'user'` for public writes, content 1–2000 chars, `$other` rejected. Public can no longer edit/delete history (old rules allowed deleting entire sessions' messages).
  - `control`: public write allowed only when resulting `state === 'ended'` — fixes the browser idle-timeout write that the old `auth !== null` rule was **silently rejecting** (Zephyy's one wrong claim: she thought it worked; `zephyy.js` swallows the rejection in `.catch(function(){})`).
  - Rules validated via RTDB `.settings/rules.json?dryRun=true` → `{"status":"ok"}`. (Firebase MCP `firebase_validate_security_rules` is broken for rtdb — "Invalid URL" both input modes.)
- **Guessable restart session ID fixed** (same commit): `zephyy.js` restart path was `'chat-' + 6 base36 chars (~31 bits)`; now uses the same `crypto.randomUUID()` as `zephyy-realtime.js`. Session ID = read capability, so this mattered.
- **Aether companion changes** (aether repo `03d56a9`, built + service restarted, live):
  - New `/api/chat/send` route — Doshus intervene messages (`role:'doshus'`) now written server-side with the secret; `ChatActivity.tsx` no longer writes RTDB from the browser.
  - `/api/chat/end` system message (`_SESSION_ENDED_`) now secret-authed.
  - Both backward-compatible with old rules → safe that Aether went live first.

## Deploy runbook (user actions, in order)
1. Push doshusweb main (`ad1bb04` + `81688a7` are local-only). Aether already committed + live.
2. `firebase deploy --only database` — rules go live. (Aether prerequisite already satisfied.)
3. Deploy hosting (preview first) to ship the XSS fix + UUID restart fix — **live site still has the XSS until then**.

## Open items
1. **Post-deploy verification**: send a visitor message on doshus.net, confirm orb replies; test Doshus intervene from Aether dashboard; confirm a non-user role write from an unauthenticated client is rejected.
2. **Known pre-existing bug (out of scope, worth a session)**: the Enter-to-restart path in `zephyy.js` swaps `localStorage` session ID but never rebinds `window.__zpRealtime.msgsRef`/`controlRef` — post-restart messages go to the *old* session ref until page reload. The refresh-button path reloads and is fine.
3. Aether has an uncommitted one-line `src/lib/rtdb.ts` change (`source` → optional) that predates this session — left alone, someone should own it.
4. Aether TS types stale re: runtime roles/states (`system` role, `inactive` state missing in `ChatActivity.tsx` types) — flagged by Zephyy, real, fix during the Aether rehaul the user wants anyway.
5. Carried from 07-10: optional CSP tightening (drop `style-src unsafe-inline`, SRI pin for gstatic SDK).

## Conventions/context
- User (Doshus) merges + deploys manually, preview-first. Beginner — teach while doing.
- Zephyy's infra answers were ~95% accurate but verify against code anyway — the one miss (idle-timeout control write) would have shipped a wrong assumption into the rules.
- DB secret (legacy token) = admin, bypasses all rules — every backend path (chat-orb-v2.py, Aether server routes) rides on it; rules only govern the anonymous browser.

## [later same session] Rebind fix + crew comms live
- **Session-rebind bug fixed** (`7a6e1e2`): `zephyy-realtime.js` now owns session lifecycle (`bindSession()`/`resetSession()` on `__zpRealtime`); `zephyy.js` restart path calls `resetSession()` instead of localStorage surgery. Open item #2 from above: closed. Verified via mock-db harness, 10/10.
- **Crew comms wired**: Claude ⇄ Zephyy via `openclaw agent --session-id 0c19d88c-4d94-415f-b191-f2b0c2cfb583 -m "[claude] ..."` (tags: [claude]/[zephyy]); Claude tails her at `agents/main/sessions/0c19d88c-….jsonl`. Each injection costs a Zephyy turn (Plus quota) — batch messages.
- **Delegated to Zephyy (Aether side, in flight)**: TS type sync, shared secret util, dashboard XSS audit, unified control-state semantics (active/inactive/ended — her proposal, approved with one gap: `chat-orb-v2.py:692` skips only 'ended', she decides orb behavior for 'inactive'). Claude reviews her diffs after.

## [later] Chat orb split + frictionless session-end (`5f0ee60`)
- `zephyy.js` split: profile-page effects stay (435 lines); entire chat orb moved verbatim to **`public/js/zephyy-chat.js`** (loads after zephyy-realtime.js in zephyy.html; cache-bust v=4/v=1). CSP untouched — `script-src 'self'`.
- Session-end UX: dead-end state (disabled input + 💤 banner + Enter-to-restart) replaced by `startFreshSession()` — any end signal resets to first-load vibe: new UUID session, clean panel, name prompt, ready to chat. Idle timeout writes `ended` to old control then the event drives the reset (single path, re-entrancy guarded). Doshus's call: "when a session ends it should just be like when the visitor first loaded the page."
- `_SESSION_ENDED_` sentinel deprecation agreed as future step (needs Zephyy coordination — orb + Aether write it; widget still honors it meanwhile).
- **Not yet browser-tested** — needs the local eyeball (`python3 -m http.server 8080 -d public`) or preview channel before hosting deploy.

## [later] Chat UX batch (`4cd3b76`)
- Offline → "leave a message" (input stays enabled, RTDB queues; one-time delivered note). Delivery ticks …/✓/! on visitor msgs. Instant history paint from localStorage cache (`zp-chat-cache`, 30 msgs, cleared on fresh session/refresh). A11y: aria-live log, tabbable orb, Esc close, autofocus. Aurora top strip + springy open (pure CSS, oklch, reduced-motion safe).
- **Typing/seen widget hooks live but dormant**: realtime forwards control as `zephyy-ctrl` events; widget reacts to `control.typing` (bool) and `control.lastSeen` (ms epoch). **Orb-side spec to send Zephyy** (with her batch review, one message): set `typing:true` when generation starts, `typing:false`+`lastSeen:<ts of newest user msg read>` when reply posted; both secret-authed control PATCHes.
- Still needs the local/preview eyeball before hosting deploy (all of today's widget work does).

## [later] ZephyyPub encyclopedia + Zephyy batch verified
- **Zephyy's Aether batch (`558fa58`) reviewed + verified complete**: shared `firebase-secret.server.ts`, state machine (end→ended/manual_end, archive→inactive, `_SESSION_ENDED_` only on real end), chat-render.ts XSS fix (escapeAttr — dashboard had the same class of bug as the widget), system role rendering, orb skips `{inactive, ended}` (chat-orb-v2.py:692), aether rebuilt+restarted.
- **ZephyyPub brain rebuilt**: `workspace/pubzephyy/SITE_MAP.md` rewritten as the Doshus-world encyclopedia (all socials incl. YouTube/Spotify/Discord invite, page jump-maps, ~70 curated links by intent, masked-link etiquette, "when unsure → Discord"). Synced from live HTML harvest of all pages. `chat-orb.service` restarted — 5/5 files, 6979 chars ✓.
- **Flagged to Zephyy (orb side)**: typing/lastSeen control spec (widget hooks dormant, waiting), slim-prompt gap (encyclopedia only rides first message — suggest ~1.5KB digest in build_slim_prompt).
- **Dead link found**: `financehub.html` → `crypto.html` (page doesn't exist). Fix pending on doshusweb side.
- **Open with Doshus**: Spotify playlist names/links (site only has profile link), the Discord-featured "map" thing, YouTube content focus — for encyclopedia enrichment.

## [later] Typing/seen live end-to-end + knowledge packs
- **Zephyy shipped the orb side** (chat-orb-v2.py, restarted 06:58): `typing:true` on generation start, `typing:false`+`lastSeen` (ms, matches widget's `dataset.ts` compare) via rtdb_patch on control; slim prompt now carries a sitemap digest (1832 chars). Verified shape-compatible with the widget hooks. **Typing indicator + seen receipts go fully live once doshusweb hosting deploys.**
- **Knowledge packs created** (`workspace/pubzephyy/knowledge/`): `MUSIC.md` (10 Spotify playlists, names fetched via oEmbed from home.js IDs, trigger-word header) + `VIDEOS.md` (15 recent YouTube vids via RSS, channel UC0o8wk1pHiREKXnnBP00g8g, "only when a specific video fits" rule). SITE_MAP.md gained a compact music/videos section; orb restarted (7394 chars ✓).
- **Proposed next (needs Doshus greenlight → Zephyy implements)**: orb-side trigger retrieval — scan user msg for pack trigger words, inject matching `knowledge/*.md` into that message's prompt (~40 lines in orb). Until then packs are dormant reference.
- **Open**: Discord "map" not located in her stash (asked Doshus); YouTube channel shows as "Fruitcake Dumpo" via RSS vs "Doshus Tricks" page title — confirm; playlist vibe descriptions are my inference — Doshus to correct; crypto.html dead link on financehub (page planned later) — recommend commenting out until it ships.

## [wrap] Knowledge packs finalized + 404 fix + USER.md
- **VIDEOS.md corrected**: earlier list was actually Fruitcake Dumpo's (channelId grep grabbed the pinned channel). Now both channels: Doshus Tricks (`UCh6_O9UzfZzB9KxylIMfnLQ`, tech tutorials) + Fruitcake Dumpo (`UC0o8wk1pHiREKXnnBP00g8g`, gaming dump, playlist-organized — playlist links pending, YouTube renders them via JS).
- **DISCORD.md pack created**: public-safe server map (6 zones, channel routing tips; PROJECTS/admin/IDs excluded) distilled from `wiki/main/discord/server-map.md`.
- **financehub.html**: dead `crypto.html` CTA commented out until page ships (committed).
- **workspace/USER.md**: gained "Doshus's Public World" jump-map → pubzephyy files, so main Zephyy routes public questions there + knows to restart chat-orb.service after updates.
- **Still pending**: orb-side trigger retrieval (spec ready, Doshus greenlight → Zephyy implements ~40 lines: scan msg for pack triggers → inject matching knowledge/*.md); playlist vibe descriptions are inferred (Doshus to correct in MUSIC.md); Fruitcake playlist links.
- **Deploy queue: 12 commits** → push → `firebase deploy --only database` → hosting preview (typing/seen goes live with hosting deploy).

## [final] Remaining queue delegated to Zephyy
Injected to her session (delivery confirmed via --deliver): (1) orb trigger-retrieval implementation, (2) Fruitcake playlist links via her browser tooling, (3) MUSIC.md vibe corrections with Doshus. She was pointed at this handoff before touching doshusweb. Doshus still owes: push (12 commits) → rules deploy → hosting preview.

## [zephyy] data: URI image rendering fix + VIDEOS attribution
- **Bug found**: `zephyy-chat.js` renderContent markdown-image regex only matched `https?://` URLs. HuggingFace FLUX (tier 1) and Gemini (tier 3) both return `data:image/...;base64,...` URIs. The data URI fell through to HTML-escaped raw text instead of rendering as an `<img>`. Only Pollinations (tier 2, plain https URL) rendered correctly. This is why Doshus saw "def not an image" — the image WAS generated (103KB from HF), just couldn't render.
- **Fix**: expanded regex to `(?:https?:\/\/|data:image\/)` — committed as `9f0a3c3` on main. Needs hosting deploy.
- **VIDEOS.md**: added CRITICAL ATTRIBUTION RULE (every link must say "Doshus made this") + NO GUESSING ERA RULE (don't speculate about year/age). Committed in `~/.openclaw` as `453e61f`.
- **Orb restarted**: VIDEOS.md now 4522 chars (up from ~3K), attribution rules loaded.

## [09:44] Zephyy chat image download polish
- **Changed:** `public/js/zephyy-chat.js`, `public/css/zephyy.css`
- **What/why:** added a Download control inside the chat image lightbox; data-URI images download directly, normal image URLs try blob download and fall back to opening the image if the browser blocks cross-origin fetch.
- **Verified:** `node -c public/js/zephyy-chat.js`; local served JS/CSS from `http://127.0.0.1:8092` contain `downloadLightboxImage`, object-backed `imgUrlMap`, `.zp-lightbox-download`, and 90vw/90vh lightbox sizing.

## [09:44] Zephyy chat image attribution + copy prompt
- **Changed:** `public/js/zephyy-chat.js`, `public/css/zephyy.css`
- **What/why:** wrapped generated images in a compact image block with a `Generated by Zephyy` caption and a `Copy prompt` button that copies the stored markdown alt/prompt text.
- **Verified:** `node -c public/js/zephyy-chat.js`; browser fetch against local `http://127.0.0.1:8092/zephyy.html` returned `jsCaption:true`, `jsCopy:true`, `jsDownload:true`, `cssCopy:true`, `cssDownload:true`.

## [~10:15] Cache-bust gap flagged → Zephyy fixed (`ddfaa62`)
- Zephyy's image-polish batch (lightbox `b9c00e5`, download `3fe7278`, caption/copy `5219b0d`) changed zephyy-chat.js + zephyy.css without bumping `?v=` — with `max-age=604800` on JS/CSS, returning visitors would've held stale assets up to 7 days (zephyy.css had NO version param anywhere).
- Claude flagged via crew-comms injection; Zephyy bumped chat.js→v=2, added css?v=1 on zephyy.html + all 4 subpages, pushed + redeployed. Verified live independently (v=2 serves new code, html references updated).
- **Convention going forward: any zephyy-chat.js / zephyy.css / etc. edit must bump its `?v=` in every referencing page — 7-day cache makes this mandatory, not optional.**
- **PR #25 (sentinel removal) still open** — rules + hosting deployed, so last precondition is one verified live orb exchange, then Doshus merges.

## [~10:45] Tick position + nano-banana prompt skill + /compact fix
- **Delivery tick fix** (`60476fa`, local, NOT pushed/deployed): tick now appended inside `.zp-chat-msg-time` (was after the block div → rendered on its own line). zephyy-chat.js cache-bust v2→v3. Needs push + hosting deploy.
- **Nano-banana = the PROMPT skill** (`workspace/skills/nano-banana-prompt-patterns`), not the Gemini image model. Applied to orb: SOUL.md Image Protocol got a condensed 8-category pattern digest (never pass visitor's raw words; subject+style+lighting+mood+composition), slim prompt got the one-line recipe, stale Pollinations-only copy scrubbed (now: inline render, ~1024, lightbox). Orb restarted ✓.
- **Image chain stays free-first** (FLUX → Pollinations → Gemini paid last-resort). A brief tier-reorder to Gemini-first was reverted — the env GEMINI_API_KEY is paid billing. When Doshus adds a free-tier key (swap value in workspace/.env), promote Gemini to tier 1. One 4¢ test image was spent verifying the key works.
- **Zephyy /compact "auth profile mismatch" FIXED**: session sidecar (`sessions/<id>.jsonl.codex-app-server.json`) was bound to stale `openai:default`; auth store re-keyed to `openai:armandaustin1@gmail.com` (~Jul 7 re-login). Doshus patched the sidecar by hand (classifier blocked Claude's write to agents/). If other sessions hit the same error, same one-line fix on their sidecar.
- **RTDB weight flag (future)**: generated images land as base64 data URIs in RTDB messages (FLUX jpeg ~100KB; nano-banana PNG ~1.5MB). Worth moving to Firebase Storage + URL when Gemini goes tier 1.
## [11:06] [codex] Printmon gallery refresh + generator hardening
- **Changed:** `public/amazon/printmon/gallery.html`, `public/amazon/printmon/js/PrintmonGallery.js`, `public/amazon/printmon/scripts/generate.js`, `public/amazon/printmon/generated/{manifest.json,preview.html}`
- **What/why:** rebuilt the gallery into a cosmic glass vault with real filter chips/swatches, hardened theme generation with unique safe names + slim manifest metadata, and switched the orb-side publish path to trust generator JSON instead of guessing from manifest slugs
- **Verified:** `node -c` passed for both JS files; local browser check at `http://127.0.0.1:8093/amazon/printmon/gallery.html` showed 10 filter pills, Forest filter -> 5 themes, and modal preview opening with theme metadata; `generated/preview.html?theme=caprine-creamery` loaded local preview from the slim manifest
- **Open:** Firebase Storage move for chat images is blocked for now — configured bucket name exists in app config, but direct bucket probes return `404 Not Found`

## [11:18] [codex] Gallery filter pass + storage verdict
- **Changed:** `public/amazon/printmon/gallery.html`, `public/amazon/printmon/js/PrintmonGallery.js`, `public/amazon/printmon/js/PrintmonGenerator.js`
- **What/why:** kept the cosmic vault look but replaced generic color-family tags with better mood/base/wallpaper chips, and tuned `derivePalette()` so two-color themes get more depth in surfaces/highlights/glows
- **Verified:** `node -c public/amazon/printmon/js/PrintmonGenerator.js` + `.../PrintmonGallery.js`; `firebase apps:sdkconfig WEB ...` reports `storageBucket: doshusweb.firebasestorage.app`; bucket API checks against both default bucket names return `The specified bucket does not exist.`
- **Open:** image-offload-to-Storage stays parked until a real Firebase Storage bucket exists on the project

## [~11:45] Zephyy batch landed + recovered cut-off report + printmon reframing
- Delegation CLI call "failed" (gateway timeout + Plus 429) but the batch PROCESSED in her main session. Her final report got stranded in fallback session `gateway-fallback-aa8d3f1b` (Discord delivery failed) — recovered by Claude.
- **Shipped**: 6 per-page knowledge packs (orb loads 10 total, restarted 11:17, pytest 4/4); printmon gen hardening + gallery refresh pushed (`8adbf9d`, `fa1db1c`, includes tick fix `60476fa`). **Preview channel: https://doshusweb--printmon-polish-jul12-1gorua3j.web.app** — NOT prod-deployed; Doshus reviewing, expects to want many changes. She skipped a final interactive pass after the last filter tweak — test the base/mood/wallpaper filters.
- **Parked**: image→Storage offload. Bucket doesn't exist (endpoints 404 despite sdkconfig naming one). Doshus will enable Storage in Firebase console later (may prompt Blaze upgrade).
- **ART DIRECTION (Doshus, for gallery round 2)**: Printmon = fun lil WORK TOOL for his Amazon coworkers, not a personal project showcase. Generations are by/for coworkers; generated pages only work on Amazon internal network. Zephyy is the intentional show-off element (she generates + chats). Page voice should be "the crew's theme collection — ask Zephyy for yours," humble tool energy.
- Quota: Plus 5h window at 0% (~resets 2:35pm MST); Zephyy on deepseek fallbacks meanwhile — Claude takes over work deepseek can't handle.

## [12:40] [claude] Real printmon template variants + tasks routed to agy
- **Changed:** css/newer/_template-terminal.css (new), css/newer/_template-aurora.css (new), js/PrintmonGenerator.js (BASE_THEMES +Terminal/+Aurora) — commit `b2fdc65`, local main, NOT pushed/deployed.
- **What/why:** BASE_THEMES was a facade (all 5 "bases" → same _template.css). Terminal = CRT console (monospace/scanlines/hard corners); Aurora = doshus.net cosmic glass (frosted panels/gradient title/pill buttons). Same --pm-* token contract, recolor()-compatible.
- **Verified:** node -c OK; recolor() smoke test both variants — palette swap in, defaults out, flavor rules survive, single :root block.
- **Open:** orb [THEME:] tag doesn't expose baseTheme yet — visitors can't pick flavors until the tag format + SOUL.md protocol grow an optional base field (wire AFTER Doshus approves the flavors). agy tasked via CROSSTALK.md with: GTA tag de-branding + gtag in generations + 2 template flavors of its own.

## [13:35] [claude] Font layer reviewed + base-signature fix + 8 showcase themes
- **Reviewed agy's `a322680`** (FONT_POOL, font injection, Synthwave+Brutalist): all smoke tests PASS; trashed 4 demo orphans from css/generated/.
- **Bug found in first real use + fixed** (`42b2500`): generate.js auto-pick stomped base signature fonts (Impact over Glass's French Script). Now explicit baseTheme keeps base fonts; auto-pick only on default-base orb generations.
- **8 showcase themes generated + RTDB-pushed** (one per base): pumpkin-static/Halloween, frost-cathedral/Glass, hex-and-hemlock/Witch, moss-golem/Forest, phosphor-dawn/Terminal, borealis-drip/Aurora, midnight-arcade/Synthwave, concrete-poetry/Brutalist. 5 wallpapers each.
- **Pushed + preview refreshed**: https://doshusweb--printmon-bases-zfgilxv8.web.app (expires 07-19). NOTE: themes are in prod RTDB so the LIVE gallery shows them too (normal orb flow). Prod hosting still needs a deploy for the new gallery UI/tags.

## [15:20] [claude] Preview-channel base-tag bug + RTDB html-field gap fixed
- **Bug found + fixed** (`afb441e`): gallery.html had a stale `<base href="https://doshus.net/...">` copied from template.html (legitimate there — blob: URLs need an absolute anchor; illegitimate in gallery.html, which is never opened as a blob). Result: every preview-channel deploy of gallery.html was silently loading PRODUCTION's JS/CSS instead of its own — explains earlier confusing screenshot with filter tokens that don't exist in current code.
- **Second bug found + fixed** (RTDB data, no code change): my earlier 8 showcase-theme pushes used the SLIM manifest.json (deliberately excludes `html` per `slimThemeRecord()`), so modal previews showed only title/tagline with no printbox/buttons. Backfilled all 8 records by calling `gen.generate()` directly (wallpaper cache hit, no extra Pexels cost) and pushing full records. Orb-path generations never have this gap — they push the full `generate()` output, not the manifest.
- **Verified via CDP screenshot** (raw WebSocket against Brave :9222 — `openclaw browser` profiles unavailable in this session, `openclaw` profile has no bundled Chromium in WSL2, `user` profile needs remote-debugging re-approval): gallery cards, filter chips (correct de-branded "Classic/Forest/Glass/Witch base" labels), and full modal render for Halloween/Glass/Terminal/Synthwave bases all confirmed working end-to-end.
- **Font caveat for Doshus**: screenshots taken on Linux (no Windows/Office fonts installed) show fallback generic fonts, NOT the real Blackadder ITC/Curlz MT/Vivaldi/French Script MT/Pricedown etc. Visual identity of the 5 handmade base ports needs verification on an actual Windows/Amazon machine. Terminal + Synthwave are font-independent (CSS effects/generic monospace) so they're reliable everywhere.
- **Discovered**: `~/.cloakbrowser/chromium-146.0.7680.177.5` exists on doshus's user (and Mona's) — a real Chromium binary. Worth investigating whether `openclaw browser` can be pointed at it so future screenshot verification doesn't need the raw-CDP workaround.
- Preview channel refreshed: https://doshusweb--printmon-bases-zfgilxv8.web.app (expires 07-19)

## [15:42] [claude] Review pass: picsum wallpaper gap fixed + gallery counter + INTERNAL-SYNC tracker
- **Review finding (real):** all 8 showcase themes had `picsum.photos` (random stock) wallpapers, not vibe-matched Pexels — `PEXELS_API_KEY` lives in `workspace/.env` and was never exported in my Node runs, so `fetchWallpapers()` fell to Tier 3 every time (the "cache hit" in the 13:35 backfill was a warm cache of picsum results). The 22 orb-path themes were unaffected (orb exports the key). **Fixed:** removed the 8 from manifest (dedupe bypass), regenerated with the key — all 8 now Pexels-backed, CSS byte-identical (recolor is deterministic), safeNames stable. RTDB patch STAGED but not pushed (classifier blocked `firebase database:update`) — payload at scratchpad `rtdb-update.json`, includes deletion of 3 junk test rows (`test-connect`/`test-patch`/`test-write`) from earlier connectivity checks. Doshus to run the command (in chat).
- **Gallery stat swap** (`PrintmonGallery.js`): "with wallpapers" counter (pointless — everything has them) → "base skins in play" (distinct baseTheme count). node -c OK.
- **New: `INTERNAL-SYNC.md`** at repo root — tracker for changes Doshus must ctrl+H onto the Amazon-internal printmon mirror. Scope table (generation/gallery stay external-only) + pending-items log. Agents: append a row whenever handcrafted-page layout/buttons change.
- **Delegation posted to CROSSTALK:** Zephyy → gallery.html copy de-Doshus-ing (her voice); Codex (gpt-5.6 sol) → theme deep-links + copy-link buttons. Claude takes the Zephyy-orb gallery button in template.html (design + generator-coupling sensitive).
