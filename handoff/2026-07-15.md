# 2026-07-15

## Main-site CSP tightening and Firebase SDK SRI

- Moved Zephyy profile whorl animations from JS-injected styles into
  `public/css/zephyy.css`; removed the generated SVG `style` attributes from
  both profile and chat-orb glyphs.
- Removed the widget's runtime style injection, moved its effective animation
  rules and SVG transform origins into `public/css/zephyy-widget.css`, and
  cache-busted all changed Zephyy CSS/JS references.
- Extracted the changelog, crew, QA, and status inline style blocks byte-for-byte
  (whitespace aside) into page-specific `public/css/zephyy-*.css` files.
- Removed `style-src 'unsafe-inline'` from both root hosting CSPs in
  `firebase.json` and added SHA-384 SRI plus anonymous CORS to all four
  Firebase 11.0.0 compat SDK tags.
- Main-site JS audit found and removed three generated SVG `style` attribute
  patterns; no other `innerHTML` or `insertAdjacentHTML` style-attribute
  insertions were present.

## Verification

- All `public/**/*.js` files pass `node -c`.
- `firebase.json` parses as JSON; both root CSP `style-src` directives omit
  `unsafe-inline`.
- Main-site grep proofs are clean for `createElement('style')`, HTML `<style>`
  tags, and generated JS `style=` attributes.
- The four SRI values match freshly downloaded version-pinned gstatic files.
- `git diff --check` passes. No server, emulator, push, or deploy was run.

## Zephyy subpage navigation sync

- Added the idempotent root-level `sync-zephyy-nav.js` stamper as the canonical
  source for the four subpage navs and the Zephyy profile footer links.
- Merged Crew, Pipeline, Changelog, Status, and Doshus into each top subpage
  bar with page-specific active state; removed the four duplicate sticky
  footers while preserving the profile footer orb and copyright.
- Kept the top links visible on mobile with a compact horizontally scrollable
  bar, added active/accent and favicon alignment styles, and set
  `zephyy-subpage.css?v=1` on all four consumers.
- Verified root/public JavaScript syntax, two-run stamper idempotence, exact
  link and active-state counts, zero subpage sticky footers, and clean diffs.
  No server, push, or deploy was run.
