# 2026-07-16 — Sitewide Zephyy orb + haptics ship day

## What shipped (PRs #26, #27, #28 → main, deployed)
- **PR #26**: shared `haptic(ms)` helper in main.js; footer nav, collapsibles,
  modals, meme-video controls, home.js buttons (Discord/CTA/Slack/pills) all tap.
- **PR #27**: printmon gallery orb repositioned below the Doshus.NET button
  (tooltip collision with the strip's upward data-tooltips).
- **PR #28** (the big one, built by Agy — Opus 4.6 → Gemini 3.1 mid-task — with
  4 review rounds + fix-ups by Claude Code):
  - Sitewide orb embed (`zephyy-orb-embed.css/js`): desktop dim corner-dock,
    mobile teal status-dot (ping every 4s) → tap expands to whorl-avatar orb.
  - Full chat panel + Firebase stack stamped on all 5 non-profile pages —
    Zephyy is chattable everywhere, page-aware via existing meta/page plumbing
    (404 blurb added orb-side in openclaw repo).
  - Chat CSS extracted `zephyy.css` → shared `zephyy-chat.css` (10 consumers);
    whorl glyph rules moved there too.
  - Conversation continuity: active session ⇒ orb auto-presents on every page
    (expanded/full-opacity, quiet arrival). Sessions already persisted via
    localStorage + RTDB; killers are refresh button, 30-min idle, orb end.
  - 6 rotating chat intros; header name links to /zephyy; refresh is 2-tap
    confirm (amber arm, 3.5s).
  - Printmon orb = same shared component (whorl via swap-img.js, 19 quips).
  - Perf/a11y: reduced-motion respected throughout; footer-sync scroll-hide.
  - Fixes en route: orphan </div> ×5 (agy stamper), CSS brace pair bug (both
    halves), panel unstyled on non-profile pages, sticky :hover on lounge
    filters (@media hover:hover), tap-highlight now covers buttons sitewide,
    status-dot teal captured at :root (panel recolor scoped override).

## Conventions added
- **CSS brace-balance check** added to CLAUDE.md verify ritual (browsers don't
  error on unbalanced braces — caught 2 real bugs the day it was added).
- PR workflow now standing for doshusweb: branch → PR → Doshus reviews →
  merge/deploy on his go-ahead.
- Cache busters after today: main.js v3, home.js v1, shared.css v1, lounge.css
  v1, zephyy.css v11, zephyy-chat.css v5, zephyy-chat.js v9, zephyy.js v10,
  orb-embed css/js v3, swapbtn orb6, swap-img orb7.

## Internal mirror (Amazon)
- INTERNAL-SYNC.md: 2026-07-16 row added (orb rebuild supersedes 07-15 batch).
- INTERNAL-SYNC-PROMPT.md fully rewritten for Quick Suite — self-contained
  payloads (internal gets orb rules inlined in swapbtn.css; no zephyy-orb-embed
  file internally, no cache busters, classics still skip the orb).

## Open items
- Preview channel `orb-preview` (both targets) expires 2026-07-23 — can be
  deleted after production verification.
- Printmon whorl/orb must be eyeballed ON PRODUCTION (base href prevents
  preview-channel testing of printmon pages).
- Backlog (Doshus-gated): shader hero prototype, AMAZON.md knowledge pack
  (his to write), possible orb expansion beyond current pages.

## Late additions (same day, post-compact)
- **PR #32** (merged + deployed): one-line touch-safe tooltip — nowrap/max-content
  CSS, touchstart lift, real-width viewport clamp. Doshus still saw clipping +
  phantom vertical scroll on the Fold → root cause: sticky :hover re-showed the
  CSS absolute fallback after the JS reset (plus the tip sat inside the strip).
- **PR #33** (merged + deployed): orb moved ABOVE the Doshus.NET button
  (dock `column-reverse`, still in flow), tooltip pops UP above the strip
  (JS fixed lift, measured + clamped), CSS hover fallback gated
  `@media (hover: hover)` so touch can't re-trigger the clipped version,
  touch lifecycle = show on touchstart / auto-clear 1.6s after touchend.
  Button tooltip may overlap the orb — Doshus accepted that trade.
- INTERNAL-SYNC-PROMPT.md + INTERNAL-SYNC.md updated in lock-step both PRs;
  Doshus says he's applying the Quick Suite sync soon — prompt reflects the
  FINAL orb-above state (skips intermediate iterations).
- NOTE: cache-buster inventory listed under "Conventions added" above is
  obsolete — all `?v=` busters were dropped sitewide (PR #30). Verification
  on device needs a hard refresh / incognito.
- Awaiting Doshus's on-device verdict on orb-above; no further contingency
  planned (in-flow placement, both prior bug classes structurally closed).
