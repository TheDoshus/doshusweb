# Handoff — 2026-07-17

## Internal mirror: fully synced ✅

Doshus + Quick Suite applied the whole INTERNAL-SYNC backlog to the Amazon-internal
printmon mirror (orb component, dropdown entries, link repairs — everything through the
2026-07-16 orb-above batch). All pending rows moved to Synced history in `INTERNAL-SYNC.md`;
`INTERNAL-SYNC-PROMPT.md` header now marked applied (file kept as the template for the
next batch).

**Wording rule (both sides):** internal copies were ctrl+H'd **skin→theme / skins→themes**;
external swept to match same day (PR #35: tooltip default on 31 pages + template, 3
swap-img.js quips, gallery.html copy ×3, PrintmonGallery.js stat label). User-facing copy
is theme/themes everywhere now — write new strings that way. Deliberately kept: `skin`
stopword in PrintmonGenerator.js's prompt-cleanup regex (functional), `<video-minimal-skin>`
tags in the tmb tutorial (unrelated custom element).

## Wallpaper pools ported from internal

Doshus updated wallpaper arrays internally and handed them over — applied externally:
- **GTA**: 49 rockstargames.com GTA VI stills/videos (47 jpg + 2 mp4) → `TheDoshusPrintmon2GTA.html`
  (10 local + 49 remote) and `Printmon2DoshusVI.html` (rockstarintel webp kept + same 59).
- **Glass**: 12 pixabay `_large.mp4` videos appended to the existing 19 smalls →
  `TheDoshusPrintmon2Glass.html` (31 total).

All 61 remote URLs verified reachable via range requests; the pages' wallpaper scripts
already handle mp4 (video element path). No CSP change needed — `/amazon/**` carries the
permissive legacy policy in firebase.json. No internal sync needed (internal is the source).

## Feedback quips + gallery ideas (in flight)

5 new orb quips added (24 total): Zephyy takes theme feedback/requests + tampermonkey
script ideas. **Needs internal sync** — row added to INTERNAL-SYNC.md with paste-ready lines.

## Gallery hero orb + chat (BUILT, in PR #35)

Doshus greenlit; all landed:
- Hero h1 → "Your vibes. Zephyy's themes. Our gallery."; hero-note now invites orb feedback
  (themes, generations, tampermonkey ideas).
- Big Zephyy orb (`gallery-dock` variant) in the hero between copy and stats — 96px desktop,
  72px phone (wraps below copy), always-visible "toss me a vibe ⚡" caption, opens the standard
  chat panel. CDP-verified pre-deploy on the live page (desktop + 390px: in-hero, no overflow).
- Full zp-chat block added to gallery.html (`../../` paths); gallery doesn't load shared.css,
  so the chat-needed tokens (brand triples, text-main/muted, space-oled) are declared in its
  inline styles. CSP fine — `/amazon/**` permissive.
- `zephyy-orb-embed.css`: new gallery-dock section; mobile collapsed-dot chains now also
  `:not(.gallery-dock)`. `zephyy-orb-embed.js`: mobile tap-to-expand intercept skips
  gallery-dock (first tap opens chat).
- **Backend (outside repo, LIVE)**: `~/.openclaw/scripts/chat-orb-v2.py` — new exact
  PAGE_CONTEXT entry for `/amazon/printmon/gallery.html` (theme-gen priority, honest about
  hit-or-miss generations vs handcrafted, graceful with complaints, welcomes feedback +
  tampermonkey ideas); printmon prefix entry reworded (skins→themes); gallery triggers the
  printmon knowledge packs. `chat-orb.service` restarted 01:34, up clean.
- Gallery is external-only (never mirrors internally) — no sync row.

## Gallery polish round 2 (PR #36 — feat/gallery-preview-polish)

Doshus merged+deployed #35 himself and added the internal tooltips. New round, all built
and CDP-verified live pre-deploy:
- Preview modal: View source button removed (+ its dead JS), base/wallpaper pills removed
  (+ .preview-meta/.theme-chip CSS; grid-card chips untouched), header tightened to one slim
  row (title + tagline beside it, 53px vs ~95px — iframe now ~91% of the modal).
- Copy link now copies the **full-page view**: share URL gains `&view=full`; on load with
  that param PrintmonGallery.js document.write()s the standalone theme page (Blob pages
  aren't linkable), keeping the shareable URL in the bar.
- Chat is now **inline in the hero** instead of the floating overlay: panel markup moved
  inside the gallery-dock wrapper (column layout), display-gated static panel
  (min(480px,100%) × 420px) expands under the orb on click; backdrop element deleted from
  gallery (all zephyy-chat.js refs are guarded). Overrides live in the gallery-dock section
  of zephyy-orb-embed.css.
- Gallery is external-only — no internal sync rows.

## Theme-matched Swap button (PR pending — feat/swapbtn-theme-accents)

Long-standing wish of Doshus's: the Swap Themes button now wears each page's palette.
- `swapbtn.css`: `.dropbtn2` routed through `--_swap: var(--swap-accent, var(--pm-hue1, gold))`;
  alpha (92%) + glow (50%) applied centrally via color-mix so per-page vars stay plain colors;
  hover brightens toward white (replaces the old gold→purple jump). Ink defaults white,
  `--swap-ink` for light accents.
- All 31 theme css files got a `:root` accent block prepended (accent from each theme's h1
  palette, glow from its text-shadow companion; dark ink: Bitcoin, Butterfly, Spongebob).
- Generated pages auto-match via their existing `--pm-hue1` token — zero generator changes
  (generator bases are separate `_template-*.css`, so the prepends can't collide with
  recolor()'s :root replacement).
- CDP-verified with screenshots on live Melody (pink), Spongebob (yellow + dark ink),
  Kuromi (plum). Gotcha for future measurement: the button has `transition: all 0.3s` —
  wait ~600ms after injecting CSS before reading computed styles.
- **Internal sync pending**: INTERNAL-SYNC-PROMPT.md fully rewritten for this batch
  (button payload + all 31 prepend blocks verbatim); row in INTERNAL-SYNC.md. Doshus
  synced the quips himself earlier — moved to history.

## Next up (Doshus)

Polishing pass over the external printmon pages is coming soon — he'll review the orb
code for optimization/organization first.

## State

- PRs #32/#33/#34 merged + deployed; orb confirmed good by Doshus on-device.
- Sync sweep (07-16) all green: template dock markup byte-identical to the 17 strip
  pages, 14 classics uniform, gallery/generated correctly orb-free, all 32 orb pages +
  template reference `zephyy-orb-embed.css`.
- Doshus plans a personal code-review pass over the orb code for optimization/organization
  (`css/zephyy-orb-embed.css`, `js/zephyy-orb-embed.js`, printmon `js/swap-img.js` orb
  IIFE, `zephyy-chat.css/js`) — expect follow-up change requests; keep it lean.

## Open items (carried, Doshus-gated)

- Shader hero prototype; AMAZON.md knowledge pack (his to write); orb expansion ideas.
- Preview channel `orb-preview` expires 2026-07-23 (deletable).
- Chat-orb XSS hardening in `renderContent()` (zephyy.js) — pre-existing backlog, see DOSHUS.md.

## Theme-matched dropdown (PR pending — feat/swap-dropdown-theming)

The Swap Themes dropdown now follows the button's accent routing:
- `--_swap` moved from `.dropbtn2` up to `.dropdown2` (wrapper) so button AND menu inherit it.
- Panel: accent-tinted dark glass (20% accent into oklch(24% 0 0 / 0.55)), accent border,
  soft glow via the theme's `--swap-glow` companion.
- Links: pastel of the glow companion (55% toward near-white — always readable); hover =
  translucent accent highlight. Replaces the fixed green #74EB12 / #3F832C.
- Scrollbar: accent-tinted thumb, dark track (was light-gray, off on dark glass).
- Animation: `swapMenuIn` slide-down + overshoot (0.28s, transform-origin top left)
  replaces plain fadeIn; `.show` added to the reduced-motion block. Shared2Printmon.css
  has its own fadeIn copy, and no classic css uses fadeIn — safe removal.
- CDP-verified w/ screenshots on live Melody (pink glass), Kuromi (plum), OP (warm brown,
  pastel-blue links). Gotcha hit again: earlier tabs had CACHED pre-#37 theme css (no
  ?v= busters) — production CDP checks must always set Network.setCacheDisabled.
- Internal: INTERNAL-SYNC-PROMPT.md rewritten (batch 2) — single-section replace in
  swapbtn.css; batch-1 row moved to Synced history (Doshus synced it same day).
