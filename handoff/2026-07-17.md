# Handoff — 2026-07-17

## Internal mirror: fully synced ✅

Doshus + Quick Suite applied the whole INTERNAL-SYNC backlog to the Amazon-internal
printmon mirror (orb component, dropdown entries, link repairs — everything through the
2026-07-16 orb-above batch). All pending rows moved to Synced history in `INTERNAL-SYNC.md`;
`INTERNAL-SYNC-PROMPT.md` header now marked applied (file kept as the template for the
next batch).

**New standing divergence:** internal copies were ctrl+H'd **skin→theme / skins→themes**.
External pages still say "skin" (orb quips in `swap-img.js`, tooltip default in the dock
markup). Any future sync payload with user-facing "skin" strings must swap to "theme"
wording internally — noted in INTERNAL-SYNC.md scope rules and the prompt header.

## State

- PRs #32/#33/#34 merged + deployed; orb confirmed good by Doshus on-device.
- Sync sweep (07-16) all green: template dock markup byte-identical to the 17 strip
  pages, 14 classics uniform, gallery/generated correctly orb-free, all 32 orb pages +
  template reference `zephyy-orb-embed.css`.
- Doshus plans a personal code-review pass over the orb code for optimization/organization
  (`css/zephyy-orb-embed.css`, `js/zephyy-orb-embed.js`, printmon `js/swap-img.js` orb
  IIFE, `zephyy-chat.css/js`) — expect follow-up change requests; keep it lean.

## Open items (carried, Doshus-gated)

- Shader hero prototype; AMAZON.md knowledge pack (his to write); orb expansion ideas.
- Preview channel `orb-preview` expires 2026-07-23 (deletable).
- Chat-orb XSS hardening in `renderContent()` (zephyy.js) — pre-existing backlog, see DOSHUS.md.
