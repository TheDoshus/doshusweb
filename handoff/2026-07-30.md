# doshusweb handoff — 2026-07-30

## Zephyy profile v4 (branch `feat/zephyy-htmx-profile-refresh`)

- **Changed:** rebuilt `/zephyy` into seven focused sections; added four static HTMX
  fragments and a four-state mood lens; refreshed the May→July flight recorder; removed
  the duplicate sticky footer from this page; tightened chat-dialog accessibility and
  realtime null/race handling; vendored HTMX 2.0.10 with its Zero-Clause BSD license;
  narrowed both hosting CSPs to self-hosted HTMX and the pinned Firebase 11.0.0
  source-map path; removed one stale inline-script hash through the repo's CSP sync.
- **Why:** the public profile had become a stack of stale adjacent widgets. The new page
  reads as a current operator profile while HTMX stays an optional HTML enhancement, not
  a new architecture or backend.
- **Verified:** all JS parses; the vendored HTMX file matches the pinned SHA-384 digest;
  JSON and CSS structure pass; eleven profile routes return direct 200s; changed surfaces
  use oklch only; Chrome DevTools showed four direct-200 HTMX swaps, ARIA tab arrow-key
  navigation, hydrated live state, correct chat-dialog focus/ARIA state, and a clean
  desktop/mobile console; both Lighthouse runs scored 1.00 in accessibility, best
  practices, SEO, and agentic
  browsing. Local trace: LCP 374 ms, CLS 0.02.
- **Open:** production remains untouched pending PR review and merge.
