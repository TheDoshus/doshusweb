## [16:30] [codex] Private chatorb client/rules checkpoint — not deployed
- **Changed:** database.rules.json, firebase.json, public/js/zephyy-{realtime,chat}.js, tests/chatorb-client.cjs, DOSHUS.md.
- **What/why:** anonymous-auth owner sessions, atomic input/activity writes, all unseen correlated replies, actual model badges; close legacy sessions and require admin claims outside owned chat. Companion root implementation is in /home/doshus/.openclaw-worktrees/chatorb on feat/chatorb; this branch requires both sides together.
- **Verified:** actual realtime client executed in a hermetic VM: auth, session ownership, input size, atomic write shape, reply batch/order/dedupe, reset race and auth failure PASS; both changed JS files parse; diff whitespace clean.
- **NOT verified:** Firebase rule enforcement (no Java/emulator), actual browser CSP, persisted anonymous identity, real replies/actions, independent review or deployment. The VM mocks Firebase and does not prove authorization.
- **Open:** review and emulator tests, storage/account-churn limits, seven-day cached-asset cutover, approved preview then coordinated release. No query cache-busters added. Existing main-checkout QA-page edit untouched; nothing deployed or pushed.
