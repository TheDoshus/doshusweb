## [10:05] [claude] Pre-cloud sync + stale-pointer sweep
- **Changed:** public/zephyy/qa/index.html, blueprint.md, DOSHUS.md; e237690 (qa-gate.sh rename, stranded since 09-02); outside repo: ~/.claude/settings.json auto-mode deletion rule, orphan node_modules/ trashed.
- **What/why:** QA page and ~/.claude settings said `gio trash` (retired 09-17 → trash.py); blueprint claimed GitHub Actions auto-deploy (no .github/ exists; deploys are manual) and pointed at GEMINI.md; DOSHUS.md listed 3 files deleted in a60b01f (07-10) and a chat-orb XSS already fixed (zephyy-chat.js renderContent escapes label/url/alt); node_modules held a Firebase SDK that package-lock.json (0 packages) and no code references.
- **Verified:** 21/21 public JS parse, firebase.json parses, CSS braces balanced; `.github/` absent; lockfile has no packages; node_modules refs are exclusions only (firebase.json ignore, csp-hash scan skip); trash.py exit 0, dir at ~/.local/share/Trash/files/node_modules.
- **NOT verified:** QA page not eyeballed in a browser; Sentinel Gate (line 70) and coding-standards card (line 64) on the QA page still unchecked against live; blueprint.md beyond lines 15-16 remains 2026-05-12 content.
- **Open:** repo setup for agentic work (AGENTS.md build-standards block, GEMINI.md pointer, npm run check, blueprint refresh with kanban card IDs 49f2128c) — proposed to Doshus, not started. Rollback node_modules: move it back from the trash path above.

## [11:20] [claude] Agent-ready repo: canon block, handoff format, scripts into scripts/
- **Changed:** AGENTS.md (generated `canon:build-standards` block from OpenClaw root, 5-field handoff format, intro, scripts/ row, oklch rule points at build bar 7), package.json (`memes`, `sync:zephyy`), 3 loose root scripts → scripts/ (ROOT from __dirname), sync-zephyy-nav.js template, DOSHUS.md, 4 Zephyy subpages re-stamped. OpenClaw side: canon-blocks.py opt-in projects (a8199c38), meme-workflow memory.
- **What/why:** cloud clones can't point at ~/.openclaw, so the build standards are generated here and drift-checked by OpenClaw's `canonblocks` board row; the nav stamper re-added a banned `?v=1` and reverted the profile logo to `width="25px"`; subpages still showed the pre-07-30 welcome line because the chat stamper was never re-run after 15617d2.
- **Verified:** canon-blocks live PASS incl. project:doshusweb; npm scripts run from /tmp resolve correctly; second `sync:zephyy` run changes nothing; the only page diff is the welcome line ×4; no `?v=` in any Zephyy page; meme-list.json and firebase.json CSP hashes byte-identical; 0 JS/CJS syntax failures; chatorb-client PASS. Pruned 21 local branches (stale tips: chore/preserve-handoff-link 7a71ef2, codex/embed-widget-audit-jul12 40e5f51, codex/zephyy-profile-cleanup-jul10 65ab868); stale worktree metadata pruned.
- **NOT verified:** re-stamped subpages not opened in a browser; not deployed (live subpages still say "celestial co-pilot" until a deploy); the block's "Rule N" references point at coding Rules this repo does not carry.
- **Open:** GEMINI.md pointer + VS Code instructions (check vendor docs first); `npm run check` incl. stamp-drift and oklch lint; blueprint.md rewrite with Connected systems + roadmap card IDs; Doshus.code-profile removal (identical copy lives in agent-kit); kit strip_local collapses leading indentation (card).

## [11:45] [claude] Cloud pickup — start here
- **Changed:** this entry only.
- **What/why:** the local session hands doshusweb to a fresh cloud session, which starts with no conversation context and cannot reach the kanban (`~/.openclaw/data/kanban.db`), `canon-blocks.py --write`, Firebase deploys, or Zephyy's gateway. Kanban cards for this repo: `49f2128c` seasonal page effects (Doshus's idea), `9414bd21` OG images + sitemap + humans.txt, `ec678abe` QA page claims vs live (needs the OpenClaw repo to verify). Chat-orb follow-up lives in OpenClaw `scripts/bridges/orb/README.md`, not here.
- **Verified:** repo clean and pushed at this entry; cards created and read back from the board.
- **NOT verified:** what a cloud runner can reach (network, gh auth, python/node versions); whether the OpenClaw repo checkout in the cloud includes today's commits (40 were unpushed at 11:45, including the build-standards source region).
- **Open (cloud, in order):** (1) look at `/zephyy` and its subpages rendered; (2) `GEMINI.md` + VS Code instructions pointing at AGENTS.md — check each vendor's current docs for the file names/import syntax first, the VS Code app is Doshus's first real test; (3) `npm run check`: JS syntax, JSON, CSS braces, oklch-only lint on CSS, CSP hashes current (`csp:hashes` must leave firebase.json unchanged), stamp drift (`sync:zephyy` must leave pages unchanged); (4) rewrite blueprint.md (Doshus: current one is a Gemini-generated first draft, "mid") — architecture, site map incl. /zephyy/* + Printmon + orb, a **Connected systems** section (each contract that spans repos and which side owns it), roadmap listing the card IDs above; point at shared.css instead of copying tokens, drop the branches table; (5) `git rm Doshus.code-profile` (byte-identical copy lives in the agent-kit repo, its home). Card moves and `canon-blocks.py --write` happen back on the local machine.

## [12:30] [claude] Cloud pickup Open list: render check, GEMINI.md, npm run check, blueprint
- **Changed:** d6893f6 nav stamper icon root-relative + 5 Zephyy pages re-stamped; 02931d7 GEMINI.md, AGENTS.md intro, DOSHUS.md; 2923825 scripts/check.js, scripts/lib/write-if-changed.js, the 3 generators (`--check`), package.json `check`, zephyy-chat.css (lightbox rgba/#fff → oklch tokens), AGENTS.md § Verify, DOSHUS.md; 06e15b9 blueprint.md rewrite.
- **What/why:** (1) `/zephyy` + 4 subpages rendered in headless Chromium under the real main-target CSP at 1366 and 390 wide: stamps intact, 0 CSP refusals, only defect the stamped Doshus icon hard-wired to `https://doshus.net/…` (broken wherever that host is unreachable). (2) Gemini CLI imports with `@./AGENTS.md`; VS Code needs no file (Copilot/Codex/Local read AGENTS.md, `chat.useAgentsMdFile` default true; Claude harness reads CLAUDE.md; vscode-docs approved 2026-09-17), so no copilot-instructions.md copy. (3) `npm run check`: 7 checks, generators report drift instead of writing. (4) blueprint: architecture, site map, orb, Connected systems (11 contracts, owner each), roadmap cards. (5) HELD: `Doshus.code-profile` is not identical to agent-kit's pushed HEAD 9e95e24; this copy carries 17 settings + UI state agent-kit lacks, so `git rm` would lose them unless Doshus's local agent-kit has them unpushed.
- **Verified:** Gemini import via gemini-cli-core 0.61.0 `processImports`: full AGENTS.md inlined, 0 import errors, backticked `@` stays literal. `npm run check` all PASS in 1.3s; one mutation per check in a scratch copy turned all 7 red with correct file:line, `#fade` selector stayed clean; generators without `--check` report all synced; chatorb-client PASS. Blueprint rows read from both repos (chatorb.py, zephyy-status-ping.sh, daily-pulse template, ISSUES.md BUG-007, orb/README.md). Vendor docs read from their GitHub repos (egress blocks the doc sites).
- **NOT verified:** live RTDB data on any page (egress blocks gstatic/firebaseio here, so status/daily/orb ran with no SDK); Safari/iOS; mobile `/zephyy` scrollWidth drifts to ~444px from an animated layer (Chromium touch/wheel can't pan: body `overflow-x: clip` holds), not tested on WebKit; Gemini CLI and VS Code never actually launched on this repo; lightbox color change not eyeballed (same values); not deployed.
- **Open:** Doshus: decide the code-profile (push agent-kit's local copy then `git rm`, or keep this one). QA page gate cards wrap raggedly at desktop (flex-wrap with arrows as items, `zephyy-qa.css`); crew page states a model chain with Claude Haiku as the last fallback hop, which reads against OpenClaw's claude-cli-never-a-fallback policy (both → card `ec678abe`). 18 SVG icons carry hex (baseline list in check.js, shrink-only). Card moves + `canon-blocks.py --write` stay local.

## [12:41] [claude] Ownership rule: Zephyy's pages agent-owned, rest Doshus-reviewed
- **Changed:** AGENTS.md § Conventions (5c78abc).
- **What/why:** Doshus's call: agents clean up zephyy.html, public/zephyy/, zephyy-* CSS/JS freely; every other page is his hand-crafted code, smallest diff, his review first. Code profiles are his cross-laptop sync source (downloaded from GitHub); he wants agent-kit to carry agents/skills/tools the same way.
- **Verified:** npm run check all PASS after the edit.
- **NOT verified:** MDN Observatory / securityheaders.com grades (both egress-blocked from the cloud); last recorded grade is DOSHUS.md's A+ 105/100 from 2026-07-10.
- **Open:** code-profile home: copy doshusweb's newer profile into agent-kit, then git rm here (needs Doshus's OK to push agent-kit).

## [13:07] [claude] Security grades recorded; style-src unsafe-inline settled
- **Changed:** DOSHUS.md § Security / CSP Playbook.
- **What/why:** Doshus ran both scanners (cloud egress blocks them) and handed over the output: Observatory A+ 110, 11/12 (only SRI fails, -5: gtag/js + CoinGecko widget, unversioned vendor URLs that can't be hash-pinned); securityheaders.com A+, six headers green. Recorded with the date, plus Doshus's call that `style-src 'unsafe-inline'` stays (widgets need it, already tested) so no agent retries it.
- **Verified:** grades read from the scan output Doshus provided (Observatory scan 123646546 at 2026-09-26T19:42Z; securityheaders report 19:43 UTC); npm run check PASS.
- **NOT verified:** the scans were not run from this session; whether self-hosting the CoinGecko widget could earn the SRI points (his page, index.html:292, proposal only).
- **Open:** none new.

## [13:23] [claude] CSP standard enforced by npm run check; live 404 "Go Back" is dead
- **Changed:** scripts/check.js (new `csp stays strict` check + `CSP_EXCEPTIONS`), AGENTS.md § Hard rules CSP bullet + § Verify.
- **What/why:** Doshus's standard for every agent: keep the site's security tight, no unsafe/inline sources unless absolutely necessary. The check fails on any `'unsafe-*'`, `*` or bare-scheme CSP source outside the named exceptions (style-src 'unsafe-inline', img-src data:/https:), on the two targets' strict CSPs differing, and on inline `on*=` handlers / `javascript:` URLs outside Printmon. It found one live bug: public/404.html:24 `href="javascript:history.back()"`, which the CSP refuses.
- **Verified:** on production, headless Chromium clicked "← Go Back" on https://doshus.net/claude-404-probe: URL unchanged, console "Refused to run the JavaScript URL because it violates … script-src". Mutations in a scratch copy (unsafe-eval, connect-src *, target mismatch, onclick) each flagged. With Full access, /zephyy/status rendered live RTDB data (ONLINE, services active) on the branch's pages; the only 404 is /__/firebase/init.json, which only real Firebase Hosting serves.
- **NOT verified:** the 404 fix itself (not applied: Doshus's page, proposal in chat); WebSockets through this container's proxy (500 on upgrade, SDK fell back to long-polling); Safari.
- **Open:** `npm run check` stays red on 404.html:24 until Doshus approves the fix. Code profile: Doshus keeps Doshus.code-profile and will re-import from VS Code; handoff item (5) is closed, not done. Cloud env now Full network access (browser needs the proxy CA via `--ignore-certificate-errors-spki-list`: the NSS store isn't picked up and certutil isn't installed).

## [13:31] [claude] 404 "Go Back" fixed (Doshus-approved); approval rule tightened
- **Changed:** public/404.html:24 (`href="/" data-back`), public/js/main.js (3-line back handler), AGENTS.md § Conventions (recommend freely; his approval before any commit outside Zephyy's pages).
- **What/why:** the CSP refused `javascript:history.back()`, so the button was dead on production. Now it goes back when there is history and falls through to home when there isn't or JS is off. Approved by Doshus in chat before commit.
- **Verified:** npm run check all PASS (csp stays strict now green); Chromium on the branch: /nexus → /nope → click "← Go Back" lands on /nexus with no CSP refusal from the link.
- **NOT verified:** a truly fresh tab (Playwright always seeds about:blank, so history.length is never 1 there); not deployed, so production's button stays dead until the next deploy; Safari.
- **Open:** recommendation for Doshus: public/assets/vendor/videojs/video-js.min.css embeds its icon font as a data: URI that font-src refuses (logged on the 404 page and pages loading it: index, nexus, thelounge); fix by self-hosting that woff in assets/fonts/ rather than adding data: to font-src. Unverified whether any Video.js icon is actually visible on those pages.

## [13:36] [claude] Video.js data: font refusal is harmless
- **Changed:** this entry only.
- **What/why:** corrects the previous entry's Open item. main.js creates every Video.js player with `controls: false, bigPlayButton: false, controlBar: false` (public/js/main.js:330) and builds its own `.meme-video-controls`, so the VideoJS icon font is never drawn; the CSP refusal is console noise only.
- **Verified:** read main.js:326-345 and meme-video.css (only vjs-poster/vjs-tech styled).
- **NOT verified:** nothing rendered; conclusion is from the player options.
- **Open:** optional, Doshus's call: drop the unused @font-face from the vendor CSS (~7 KB of base64) to silence the refusal.

## [13:46] [claude] Video.js unused icon font trimmed (Doshus-approved)
- **Changed:** public/assets/vendor/videojs/video-js.min.css (removed its one @font-face, 48,087 → 40,805 bytes).
- **What/why:** the embedded data: woff was refused by font-src on every page loading it and never drawn (players run with controls/controlBar/bigPlayButton off; main.js builds its own controls). Approved by Doshus in chat.
- **Verified:** npm run check PASS; Chromium on /, /nexus, /thelounge, 404: 0 font CSP refusals (was 1); 8 loads of /nexus before vs after: webm memes play with no media errors either way.
- **NOT verified:** mp4 memes (Playwright's Chromium has no H.264); Safari; not deployed.
- **Open:** none.

## [13:52] [claude] Cloud setup script; RTDB rules suite runs in the cloud; session wrap
- **Changed:** DOSHUS.md § Chatorb (cloud emulator note). Outside the repo: setup script handed to Doshus to paste into the cloud environment settings (firebase-tools, database emulator pre-download, shellcheck).
- **What/why:** fresh cloud containers lack firebase-tools, so the only proof the RTDB rules hold (tests/rules-emulator.py) could not run off the rig. firebase-tools 15.31.0 routes localhost through HTTPS_PROXY ignoring NO_PROXY (lib/apiv2.js:56), which the sandbox proxy refuses ("refusing to forward injected credentials over non-TLS port"); clearing the proxy vars for that localhost-only command fixes it.
- **Verified:** script ran here in ~35s (firebase 15.31.0, shellcheck 0.9.0, emulator jar v4.11.2 cached); rules suite 51/51 PASS under the emulator; curl/node reach 127.0.0.1:9000 directly.
- **NOT verified:** the script inside the environment settings (Doshus pastes it; first new session proves it); chatorb-client/emulator not re-run after this entry (no code changed).
- **Open:** next session on the site starts from this branch (claude/doshusweb-cloud-pickup-ztttkd, not merged to main). Still open from today: QA-page gate-card wrap + crew model-chain claim (card ec678abe), 18 SVG hex baseline, cards 49f2128c / 9414bd21, 404 + Video.js fixes need a deploy to reach production.
